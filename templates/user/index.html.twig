{% extends 'base.html.twig' %}

{% block title %}{{ user.username }} - medley{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Hello {{ user.username }}!</h1>

    <p>Email</p>
    <p>{{ user.email }}</p>
</div>
{% endblock %}