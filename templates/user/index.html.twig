{% extends 'base.html.twig' %}

{% block title %}{{ user.name }} - medley{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Hello {{ user.name }}!</h1>

    <p>Email</p>
    <p>{{ user.email }}</p>
</div>
{% endblock %}